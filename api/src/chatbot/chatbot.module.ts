import { Module } from '@nestjs/common';

/**
 * Chatbot con LLM + tool calling (épica CLI-81). Por ahora solo el contrato
 * de dominio (CLI-82); el agente, el adaptador de Groq, las tools y los
 * endpoints se cablean acá en las issues siguientes.
 */
@Module({})
export class ChatbotModule {}
