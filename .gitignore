# Claude Code / AI tooling and secrets — must never be committed, anywhere in this repo.
# They still exist on disk for local dev convenience (see CLAUDE.md); git must never see them as trackable.
CLAUDE.md
.claude/
.mcp.json
.impeccable/

# macOS
.DS_Store

# Secretos y estado de los servidores (deploy/): los .env reales, los tags
# desplegados y el certificado de origen viven solo en el VPS.
deploy/**/.env
deploy/**/current.env
deploy/**/previous.env
